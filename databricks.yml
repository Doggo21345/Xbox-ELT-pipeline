# This is a Databricks asset bundle definition for Xbox ELT pipeline.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: Xbox ELT pipeline

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405609346143929.9.azuredatabricks.net

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: https://adb-7405609346143929.9.azuredatabricks.net
